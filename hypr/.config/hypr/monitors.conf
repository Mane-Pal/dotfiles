# Dynamic monitor configuration
# Run the monitor setup script on startup and monitor changes
exec-once = ~/.local/share/chezmoi/bin/monitor-setup.sh

# Default fallback - will be overridden by script
monitor=,preferred,auto,auto
